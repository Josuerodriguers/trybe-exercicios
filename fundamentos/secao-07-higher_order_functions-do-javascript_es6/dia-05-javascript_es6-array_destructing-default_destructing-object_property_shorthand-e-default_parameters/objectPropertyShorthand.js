const getPosition = (latitude, longitude) => ({
    latitude: latitude,
    longitude: longitude,
  });
  
  console.log(getPosition(-19.8157, -43.9542));

    const getPositions = (latitude, longitude) => ({
        latitude,
        longitude,
    });

    console.log(getPositions(-19.8157, -43.9542));